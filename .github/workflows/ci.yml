name: CI

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: deps
      run: |
        sudo apt install -y iverilog
    - name: build
      run: make
    - name: test
      run: make test
